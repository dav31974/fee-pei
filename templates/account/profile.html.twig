{% extends 'base.html.twig' %}

{% block title %}Modification du profil{% endblock %}

{% block body %}

    <div class="container py-3">
        <h4 class="text-center">Modification d profil</h4>

        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Enrgistrer les modifications</button>

        {{ form_end(form) }}
    </div>

{% endblock %}